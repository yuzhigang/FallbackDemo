
// 正常情况下， 每个Driver下的Read 类型的Tag都会正常读取，获取数据。
// 这里为了补偿某些Driver下没有读取到数据的Tag， 需要通过FallbackDriver来补偿读取数据。
// 基本思路就是根据Tag和Driver的对应关系， 来找到对应的FallbackDriver， 然后通过FallbackDriver来读取数据， 补偿到原来的Driver下没有读取到数据的Tag。
// 另外还存在一个情况， 整个Driver下的Tag都没有读取数据，需要通过FallbackDriver来统一补偿读取。
// 我的基本思路： 读取Driver在读取过程中如果遇到读不到数据的情况（如断开链接、读取错误、超时等），产生一个补偿任务（FallbackTask). 补偿任务可以是一个Tag的补偿，也可以是整个驱动实例（其下所有Tag）的补偿。
// 同时包含From和To的补偿范围。 另外再开一个服务，根据配置（需要根据补偿任务知道使用哪个补偿驱动实例来补偿）依次处理补偿任务， 读取数据。
// 这个思路可以使用Channel来实现，内存的生产者消费者模型。
// 我需要处理的特殊情况是： 我可能不能很好地创建补偿任务，因为我可能无法准确地知道哪些Tag没有读取到数据，哪些Driver实例没有读取到数据。需要你来帮我思考。
// 另外补偿服务是可以组合补偿任务的，实现多个补偿任务合并为一个（比如，多个采集点的from和to是一样的，我可以一次补偿多个采集点的数据读取）。
// 这里是一个类包，使用.net 8 实现。